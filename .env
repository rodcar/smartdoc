OPENAI_API_KEY=

# Default configuration for document processing pipeline
# These values will be used as defaults when providers are not explicitly specified

# OCR Provider Configuration
OCR_PROVIDER=tesseract

# LLM Provider Configuration  
LLM_PROVIDER=openai

# Vector Database Provider Configuration
VECTORDB_PROVIDER=chromadb

# Text Embedding Provider Configuration
TEXT_EMBEDDING_PROVIDER=chromadb

# Image Embedding Provider Configuration
IMAGE_EMBEDDING_PROVIDER=chromadb

# Processing Configuration
MAX_CONCURRENT_TASKS=14
